import React from "react";

import "./Home.scss";
import { Cookie, Layout } from "../../components";

export const Home = () => {
	return (
		<Layout>
            <Cookie />
        </Layout>
	);
};

export default Home;